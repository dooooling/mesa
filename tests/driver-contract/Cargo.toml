[package]
name = "forgelink-contract-tests"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
forgelink-core-types = { workspace = true }
forgelink-driver-protocol = { workspace = true }
forgelink-driver-sdk = { workspace = true }
forgelink-driver-simulator = { path = "../../drivers/simulator" }
forgelink-driver-manager = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

# TODO: Contract Test 目前仅有 M0 冒烟子集（Handshake 认证 / DataBatch 语义），
# 全量 20 项基线（§21）在 Phase 3 补齐，Simulator 必须第一个全过。
