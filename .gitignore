# Rust 构建产物
/target

# 本地运行日志
*.log

# IDE
.idea/
.vscode/

# 本地运行时数据库（ConfigStore/SQLite）
forgelink*.db
forgelink*.db-wal
forgelink*.db-shm
*.db-wal
*.db-shm

# OPC UA 证书（本地生成）
/data/
/certificates/

# Driver 构建产物（cargo build 产于 target，drivers/ 下为 QEMU/本地调试拷贝，勿入仓）
drivers/*/forgelink-driver-*
drivers/simulator/*.d
drivers/s7/*.d
drivers/focas2/*.d
drivers/opcua/*.d

# 临时 ARM/性能产物
/target
/aarch64-forgelinkd
/arm_test.sh
/arm_s7*.sh
