# SINUMERIK NCK Driver — 原生 NCK 只读（ADR 0001）。
# 经公共 mesa-s7-transport 用 Classic S7Comm ReadVar + NCK Syntax 0x82/83/84
# 直读 NCK，与 `opcua`（SINUMERIK OPC UA）、`s7`（SIMATIC S7ANY）互不重叠。
id = "sinumerik-nck"
name = "SINUMERIK NCK"
version = "0.3.0"
executable = "Mesa-driver-sinumerik-nck"
protocol_major = 1
protocol_minor = 3
sdk = "rust"
